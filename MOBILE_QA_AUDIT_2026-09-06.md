# コトダマっち iOS / Android 横断QA監査

監査日: 2026-09-06
対象: バージョン 1.0.10 の作業ツリー
判定: ソース修正と共有回帰テストは合格。Android/iOSのネイティブ実機確認が終わるまではストア提出可とは判定しない。

## 今回直した不具合

### P1: 最初の言霊後にMICがオフになる

- 共通コードの初回チュートリアル成功処理が、意図的に `stopMic()` を呼んでいた。
- Androidの `SpeechRecognizer` は一発話ごとに結果を返して終了するが、正常終了まで障害用の再試行回数として数えていた。
- Androidの一時的な `ERROR_CLIENT`、`ERROR_AUDIO`、要求過多を即停止扱いしていた。
- 再開待ち250msの間だけ `isListening=false` になり、画面復帰処理がユーザーのMICオン意思を取り消す競合があった。
- 正常な再開なのにエラーコード0をUIへ通知していた。

修正後は、チュートリアル後もMICを維持し、正常な発話区切りは再試行上限を消費せず、一時エラーだけを上限付きバックオフで再接続する。ユーザーのMICオン意思と、OSが現在録音中かどうかを分離した。

### P1: iOSの古い音声コールバックが新しい認識を止める

- キャンセル済みの `SFSpeechRecognitionTask` のコールバックが遅れて届くと、新しい認識セッションを停止できた。
- iOSイベントにはセッションIDがなく、同じ言霊を連続して唱えた時の集計状態を確実に区切れなかった。
- 利用可能判定が `ja-JP` ではなく端末の既定ロケールを見ていた。

認識世代IDで古いコールバックを無効化し、イベントにもIDを付け、`ja-JP` の認識器を直接確認するようにした。

### P1: Androidの正午通知が60秒の有効時間に間に合わない

- Android 12以降の正確なアラーム設定を宣言・確認していなかった。
- 正確な権限がない時のスケジュール警告を無視していた。
- Android画面にもiPhone専用の設定案内を表示していた。

`SCHEDULE_EXACT_ALARM`、設定状態の確認、ユーザー操作からの設定画面導線、非正確時の警告、アプリ復帰時の再予約を追加した。

### P2: ビルド・テストの取り違え

- ローカルAndroidビルドの既定バージョンが1.0.6、CIだけ1.0.10だったため、古いテスト版を誤認できた。
- Androidの雛形instrumentation testが誤ったapplication IDを期待していた。
- Android CIはAAB作成だけで、共有テスト・Gradle unit test・Lintを実行していなかった。
- iOS CIも共有回帰テストを実行していなかった。

既定値とテストを1.0.10 / `com.kotodamamatch.app` に同期し、両CIへ共有テスト、Android CIへunit testとLintを追加した。

### 追加修正: 発話・再接続・画面の残存経路

- Androidの旧`SpeechRecognizer`から遅れて届く結果やエラーが、新しい聞き取りセッションを壊せる経路を確認した。認識器ごとに世代IDを持つリスナーへ変更し、旧世代のコールバックを無視する。
- iOSで`started`通知だけを成功扱いにして再試行上限を無限に戻せる経路を確認した。実際に候補が届いた時だけ上限を戻す。また古いaudio tapが新しいrequestへbufferを渡さないよう、requestと世代IDを捕捉する。
- オンライン対戦で、作戦表示の900ms中にソケットが閉じると、送信されないまま再選択不能になる経路を修正した。自動再送はせず、作戦ボタンを復帰して明示的に選び直せるようにする。
- Android/iPadの低い横画面で「徳」ボタンが見切れる経路を修正し、全体的なピンチ拡大禁止を解除した。図鑑詳細にはdialogの名称・状態・閉じる名称を付けた。
- `jimp` を実行時依存から開発用依存へ移し、iOS CIは`npm ci`とIPA成果物の失敗を通す設定へ変更した。

## 自動検査結果

- `npm ci`: lockfileから依存関係を再構成して合格。
- `npm test`: 45件すべて合格。
- `npm run online:test`: 12件すべて合格。
- `npm run build`: ルート資産105ファイルを `www` へ同期。
- Capacitor同期後、`main.js`、`style.css`、`index.html` がルート／`www`／Android／iOSで一致することを確認。
- 再利用スキルの静的スキャナー `--strict`: エラー0件。
- skill-creator公式 `quick_validate.py`: `Skill is valid!`。
- `git diff --check`: エラーなし。改行コードの警告のみ。
- 低い横画面のDOM実測: 667×375、568×320、狭い縦画面を含む5サイズで、主要5操作すべてが画面内、横スクロールなし。
- Android Gradle compile/test: このWindows環境にJava/JDKがないため未実行。CIで実行する。
- iOS Xcode build/test: Windowsでは未実行。macOS CIと実機で確認する。
- `npm audit --omit=dev`: 本番依存の脆弱性0件。
- 開発依存を含む `npm audit`: 27件（moderate 11 / high 14 / critical 2）。主に `live-server`、`@capacitor/assets`、`wrangler` 以下の開発ツール。`--force` は破壊的変更になり、`sharp` は現時点で修正版がないため自動更新せず、別タスクでツール更新と再検証を行う。

## まだ残るリスク

### リリース前に必須

1. Android実機で、初回発話・同一語10回・無音・機内モード・着信・Bluetooth切替・画面ロック・アプリ復帰を確認する。
2. Android 12/13/14/15系の少なくとも各1台で、通知許可と「アラームとリマインダー」の全組み合わせを試し、11:59通知が12:00:59までに届くことを確認する。
3. iPhone実機で、同一語10回、通話/Siri割込み、消音、Bluetooth、バックグラウンド復帰を確認する。
4. TestFlightとPlay内部テストから入れた署名済み成果物で同じスモークテストを行う。ローカルAPK/IPAで代用しない。
5. Play Consoleでマイク型Foreground Serviceの申告、利用説明、開始から停止までの動画が登録済みか確認する。

### 要件を決めてから直す

- AndroidのForeground Serviceは通知を維持するが、音声認識器そのものはActivity側にある。メーカー省電力やプロセス終了後も無条件に待受を続ける要件なら、Serviceが認識状態を所有する設計変更が必要。
- Androidバックアップが有効で、Web Storageのランキング認証トークンと直近の聞き取りログが端末移行対象になり得る。進行データの移行を優先するか、音声・認証情報を除外するかを決めてData Extraction Rulesを設計する。
- iOSネイティブテストターゲットがない。今回の世代ID競合をSwift単体テストで再現するには、Speech/Audio依存を注入可能にする小規模リファクタリングが必要。
- Android 13以降で通知を拒否すると、マイク待受のForeground Service通知は通常の通知欄に見えにくい。MIC開始時にも通知許可を求めるか、前面利用だけに制限するか、UX/プライバシー方針を決める。

## 実機チェックの記録項目

不具合ごとに、端末名、OS、アプリversion/build、インストール元、既存データ有無、マイク/音声認識/通知/正確なアラーム権限、ネットワーク、音声経路、再現時刻、操作動画、ネイティブログを残す。再インストールやデータ消去はログ採取後に行う。

「隅々まで」は、静的監査、回帰テスト、ネイティブLint/compile、シミュレーター、実機、ストア配布物の6層をすべて通し、未実施セルをゼロにすることで近づける。ただし、端末・OS・メーカー差があるため「バグが絶対にない」とは判定せず、検証した範囲と残存リスクを毎回明記する。

## 再利用方法

- 個人スキル: `$mobile-game-quality-audit` を付けて、対象ゲームのiOS/Android監査を依頼する。
- リポジトリ原本: `skills/mobile-game-quality-audit/`
- 軽量フック: `.codex/hooks.json`。モバイルのバグ・QA・リリース依頼時だけ監査スキルの追加コンテキストを渡す。
- Codex再読み込み後、`/hooks` でプロジェクトフックの内容を確認して信頼する。
